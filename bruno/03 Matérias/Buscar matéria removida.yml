info:
  name: Buscar matéria removida
  type: http
  seq: 51
  tags:
    - atividade-05

http:
  method: GET
  url: '{{baseUrl}}/subjects/{{subjectId}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("Confirma que a matéria foi removida", function() {
          expect(res.status).to.equal(404);
          expect(res.body.success).to.equal(false);
        });

settings:
  encodeUrl: true
