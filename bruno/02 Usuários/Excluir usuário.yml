info:
  name: Excluir usuário
  type: http
  seq: 52
  tags:
    - aula-05

http:
  method: DELETE
  url: '{{baseUrl}}/users/{{userId}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("Exclui o usuário após remover seus vínculos", function() {
          expect(res.status).to.equal(200);
          expect(res.body.success).to.equal(true);
          expect(res.body.data.id).to.equal(Number(bru.getVar("userId")));
        });

settings:
  encodeUrl: true
